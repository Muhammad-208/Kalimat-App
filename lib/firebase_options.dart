import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

/// PLACEHOLDER — replace by running:  flutterfire configure
///
/// That command regenerates this file with your project's real keys and also
/// drops android/app/google-services.json in place. Until then, currentPlatform
/// throws, main() catches it, and the app runs fully offline (no sync).
class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform => throw UnsupportedError(
        'Firebase not configured yet. Run `flutterfire configure` — see SYNC_SETUP.md',
      );
}
